node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Vercel CLI local state
.vercel

# Extension dist diizinkan (built extension)
!extension/dist/

# CRX packaging output (berisi kunci rahasia key.pem — JANGAN di-commit)
extension/dist-crx/
extension/package-lock.json

# .crx publik diizinkan (extension terpaket — bukan rahasia, perlu ter-deploy)
!public/reso-extension.crx

# Firebase CLI local state
.freebuff/
