/**
 * ESM wrapper untuk shared.js — shared.js kini dual-mode (classic untuk
 * content scripts via manifest, module untuk popup/options/background):
 * tanpa statement import/export, sehingga valid di kedua loader. Konsumen
 * module (popup.js, options.js, background.js, tests) import DARI FILE INI
 * untuk mendapat named exports; file ini mengevaluasi shared.js dulu
 * (side-effect import mengisi globalThis.RS_SHARED), lalu re-export bernama.
 *
 * Menambah helper di shared.js: daftarkan di RS_SHARED + re-export di sini.
 */
import "./shared.js";

const {
  isFacebookUrl,
  isTikTokUrl,
  isInstagramUrl,
  detectPlatform,
  extractFbFeedbackIds,
  extractFbFeedbackId,
  isFacebookPostPage,
  fbTargetLabel,
  igTargetLabel,
  resolveTheme,
  svgIcon,
  iconSprite,
  injectIconSprite,
  extractInstagramShortcode,
  extractAwemeId,
  normalizeInstagramUsername,
  parseTikTokComments,
  parseIgComments,
  extractGraphqlNames,
  wordFor,
  doneMessage,
  normalizeName,
  mergeNames,
  namesToClipboardText,
  mergeAcrossPlatforms,
  filterNames,
  sortNamesAz,
  downloadTextFile,
  newRunId,
  isStaleRun,
  STORAGE_KEY_FB,
  STORAGE_KEY_TT,
  URL_TEMPLATE_KEY,
  URL_META_KEY,
  TEMPLATE_TTL_MS,
  STORAGE_KEY_IG,
  IG_TEMPLATE_KEY,
  IG_META_KEY,
  IG_TEMPLATE_TTL_MS,
  SAVED_KEY,
  PREFS_KEY,
  storageKeyFor,
  sanitizeTikTokTemplateUrl,
  isTikTokTemplateValid,
  DEFAULT_STATE_FB,
  DEFAULT_STATE_TT,
  DEFAULT_STATE_IG,
  defaultStateFor,
  sanitizeInstagramTemplateUrl,
  isInstagramTemplateValid,
  sanitizeEngineOptions,
  applyStatePatch,
  reasonToMessage,
  RESO_URL,
  RESO_DEV_URL,
  RESO_MATCH_PATTERNS,
  RSX_ENABLED_KEY,
  RESO_FIREBASE,
  jwtExpSeconds,
  getResoAuth,
  setResoAuth,
  getResoUrl,
  setResoUrl,
  resoMatchPatterns,
  applyResoConnect,
  mintResoIdToken,
  handoffResoAuthFromTab,
  ensureResoIdToken,
  sendNamesToResoApi,
  postResoEngagement,
  enqueueResoPayload,
  flushResoQueue,
  checkResoConnection,
  getResoPending,
  RESO_PENDING_KEY,
  parsePostAgeText,
  scanPageForPostDate,
  createTimeFromRehydration,
} = globalThis.RS_SHARED;

export {
  isFacebookUrl,
  isTikTokUrl,
  isInstagramUrl,
  detectPlatform,
  extractFbFeedbackIds,
  extractFbFeedbackId,
  isFacebookPostPage,
  fbTargetLabel,
  igTargetLabel,
  resolveTheme,
  svgIcon,
  iconSprite,
  injectIconSprite,
  extractInstagramShortcode,
  extractAwemeId,
  normalizeInstagramUsername,
  parseTikTokComments,
  parseIgComments,
  extractGraphqlNames,
  wordFor,
  doneMessage,
  normalizeName,
  mergeNames,
  namesToClipboardText,
  mergeAcrossPlatforms,
  filterNames,
  sortNamesAz,
  downloadTextFile,
  newRunId,
  isStaleRun,
  STORAGE_KEY_FB,
  STORAGE_KEY_TT,
  URL_TEMPLATE_KEY,
  URL_META_KEY,
  TEMPLATE_TTL_MS,
  STORAGE_KEY_IG,
  IG_TEMPLATE_KEY,
  IG_META_KEY,
  IG_TEMPLATE_TTL_MS,
  SAVED_KEY,
  PREFS_KEY,
  storageKeyFor,
  sanitizeTikTokTemplateUrl,
  isTikTokTemplateValid,
  DEFAULT_STATE_FB,
  DEFAULT_STATE_TT,
  DEFAULT_STATE_IG,
  defaultStateFor,
  sanitizeInstagramTemplateUrl,
  isInstagramTemplateValid,
  sanitizeEngineOptions,
  applyStatePatch,
  reasonToMessage,
  RESO_URL,
  RESO_DEV_URL,
  RESO_MATCH_PATTERNS,
  RSX_ENABLED_KEY,
  RESO_FIREBASE,
  jwtExpSeconds,
  getResoAuth,
  setResoAuth,
  getResoUrl,
  setResoUrl,
  resoMatchPatterns,
  applyResoConnect,
  mintResoIdToken,
  handoffResoAuthFromTab,
  ensureResoIdToken,
  sendNamesToResoApi,
  postResoEngagement,
  enqueueResoPayload,
  flushResoQueue,
  checkResoConnection,
  getResoPending,
  RESO_PENDING_KEY,
  parsePostAgeText,
  scanPageForPostDate,
  createTimeFromRehydration,
};
